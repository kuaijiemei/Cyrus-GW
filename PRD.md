# Cyrus-GW 单机 MVP PRD（面试导向版）

## 1. 项目定位与目标

### 1.1 项目级别
- 项目名称：Cyrus-GW：C++20 AI Gateway 与 Python Agent 双服务 MVP。
- 项目描述：一个面向 LLM 流式接入场景的 C++20 AI Gateway：基于 io_uring + 协程实现异步 I/O 调度，并提供 epoll Reactor 对照基线、SSE 透传、Token Bucket 限流、request_id 链路日志与 wrk 压测分析。
- 项目类型：**单机 MVP 项目**
- 开发周期：**4 周**
- 核心目标：在有限时间内做出“可演示、可压测、可讲架构”的 AI 网关项目，用于提升大厂初筛通过率与技术面试亮点。

### 1.2 目标导向（面试优先）

本项目不追求企业级完整能力，优先突出三件事：

1. **高并发处理能力**：C++20 + 异步 I/O + `SSE` 流式响应。
2. **系统架构设计**：网关层、Agent 层、LLM 调用链路清晰。
3. **AI 能力集成**：最小可用 Agent，支持任务执行与 tool 调用。

---


## 2. 范围约束（强约束）

### 2.1 必须实现

- `/chat` API
- 请求转发
- LLM 调用
- 基础限流
- 轻量 Agent（必须包含）：
  - 能接收任务
  - 能调用 LLM
  - 支持简单 tool 调用

### 2.2 最多增加 2 个增强功能

- tool 调用（已纳入）
- 简单 memory（可选，若时间允许；未纳入 `v1.0.0`）

### 2.3 明确禁止（本期不做）

- 多租户系统
- 分布式架构
- 多 Agent 协作
- gRPC
- cost 计费系统
- 复杂策略引擎

---

## 3. 目标用户与使用场景

### 3.1 目标用户

- 面试官（校招/社招一面二面）
- 技术评审者（看代码与架构）
- 项目作者本人（作品集展示）

### 3.2 核心场景

用户输入一个问题，系统通过网关转发给 Agent，由 Agent 决定是否调用 tool，再请求 LLM 并返回结果，支持流式输出。

---

## 4. 用户流程（固定流程）

**用户 → C++ Gateway → Python Agent → LLM → 返回结果**

补充说明：

- 若 Agent 判断需要工具能力，则流程为：
  - 用户 → C++ Gateway → Python Agent（tool）→ Python Agent（LLM）→ C++ Gateway → 用户
- 对外仍统一表现为 `/chat` 一条主链路。
- C++ Gateway 负责请求接入、异步 I/O、限流、转发与回包；当前版本不引入独立请求队列

---

## 5. 功能需求

## 5.1 `/chat` API（P0）

### 功能描述

- 提供统一聊天接口，接收用户 prompt。
- 支持非流式与流式（`SSE`）返回。

### 输入（建议）

- `message`：用户输入文本（必填）
- `stream`：是否流式返回（可选，默认 false）
- `session_id`：会话 ID（可选，用于简单 memory）

### 输出（建议）

- 非流式：完整 answer + 基础元信息（`request_id`、`model`、`retry_count`）
- 流式：`SSE` chunk 持续输出，结束后发送 done 事件

### 验收标准

- 可通过 curl/Postman 调通
- `stream=true` 时可逐段返回，不阻塞到全量完成

---

## 5.2 请求转发（P0）

### 功能描述

- C++ Gateway 接收 `/chat` 请求后，转发给 Python Agent Service（HTTP + JSON）。
- Python Agent Service 再统一调用 LLM provider（如 OpenAI / DeepSeek）。

### 验收标准

- 转发链路日志清晰可追踪
- 出错时返回标准错误码（4xx/5xx）与错误信息

---

## 5.3 LLM 调用（P0）

### 功能描述

- 封装统一 LLM Client，屏蔽 provider 差异（先支持 1-2 个 provider 即可）。
- 支持超时、基础重试（最多 1 次）与错误处理。
- 重试策略按 P0 实现为：**超时 + 1 次重试**（可选再加 `5xx` 触发），避免无限重试放大故障。

### 验收标准

- 正常请求成功率可稳定复现
- provider 超时或失败时有可理解的降级/报错行为
- 超时场景下可触发 1 次重试，且日志可观测（包含 `request_id` 与 `retry_count`）

---

## 5.4 基础限流（P0）

### 功能描述

- 针对 `/chat` 做单机限流（按 IP 或全局二选一即可）。
- 策略建议：令牌桶或滑动窗口，保持实现简单可讲。

### 验收标准

- 超限时返回 `429 Too Many Requests`
- 限流参数可通过配置文件调整

---

## 5.5 轻量 Agent（P0，强制）

### 功能描述

Python Agent Service 是本项目的 AI 核心模块，必须具备：

1. **任务接收**：接收来自 C++ Gateway 的用户任务（prompt + 上下文）。
2. **LLM 调用**：构建 prompt 并调用 LLM。
3. **简单 tool 调用**：识别并触发预置工具。

Agent 具备“决策能力”，能够根据任务判断：
- 是否直接回答
- 是否调用 tool
- 如何组织最终输出
Agent 决策基于规则 + LLM 输出（function calling 或 structured output）

### 最小 tool 集（建议）

- `time_tool`：返回当前时间
- `echo_tool`：回显参数（用于演示 tool pipeline）

### 验收标准

- 至少有 1 个请求路径实际触发了 tool 调用
- Agent 能将 tool 结果拼接回 LLM 上下文并生成最终回答

---

## 5.6 简单 memory（P1，可选增强）

### 功能描述

- 基于 `session_id` 在内存中保存最近 N 轮对话（如 5 轮）。
- 仅做单进程内存存储，不做持久化，不做跨进程共享。
- `v1.0.0` 只保留 `session_id` 契约与配置项，未实现实际 memory 读写。

### 验收标准

- 若实现该增强项：
- 同一 `session_id` 下第二次提问可引用前文信息
- 达到上限时按 FIFO 淘汰旧消息

---

## 5.7 并发模型压测对比（P0，面试关键）

### 功能描述

- 必须进行两套并发模型的性能对比压测：
  1. **主实现（核心）**：`Coroutines + io_uring`
  2. **对照实现（简化测试）**：`epoll + 线程池`
- 其中 `Coroutines + io_uring` 为项目主线实现；`epoll + 线程池` 仅需最小可运行测试，不要求完整功能覆盖。

### 压测范围（建议）

- 对 `/chat` 做固定请求体压测，至少覆盖并发 `100/300/500`。
- 采集吞吐、平均延迟、`P95/P99`、错误率。
- 流式场景补充记录 `TTFT`。

### 产出要求

- 输出一份对比结果（表格或 markdown），明确主实现为何选 `Coroutines + io_uring`。
- 在面试讲述中可解释两种模型的差异与取舍。

### 验收标准

- 提供可复现的压测命令或脚本。
- 提供至少一组同环境下的对比数据结果。

---

## 5.8 开发问题沉淀（P0，流程要求）

### 功能描述

- 新增并维护 `TIL.md`，用于沉淀模块开发中遇到的真实问题与解决经验。
- 每完成一个模块后，必须至少新增 1 条问题复盘记录。
- 记录必须包含：现象、根因、解决方案、防复发措施、验证方式（正常路径 + 异常路径）。

### 验收标准

- `TIL.md` 存在且可持续追加。
- 每个已完成模块在 `TIL.md` 中至少有 1 条对应记录。
- 记录内容可帮助后续开发快速复现和规避同类问题。

## 6. 非功能需求（面试重点）

### 6.1 性能目标（单机）

- 支持并发请求处理（建议压测展示：100~500 并发区间）
- 流式模式下首 token 延迟可观测（记录 TTFT）
- 在限流开启下系统不出现明显雪崩

### 6.2 可观测性（最低要求）

- 最小日志字段为**必须实现**（P0）：
  - request_id
  - latency_ms
  - path
  - status_code
  - stream
  - tool_used
  - retry_count
- 条件字段：
  - ttft_ms（仅流式成功路径）
  - error_layer（错误路径）
- 预留但本版未落地：
  - llm_call_latency_ms
  - queue_wait_ms
- 出错日志可定位在哪一层失败（网关/Agent/LLM）
- Grafana/OTel 等可视化链路为可选项（非 P0）。

### 6.3 可讲解性（面试关键）

- 架构图简洁：C++ Gateway、Python Agent、LLM/Tools 三层
- 时序图清晰：普通对话、tool 调用对话各一张
- 代码分层明确，便于现场讲“为什么这样设计”

---

## 7. 技术方案（MVP 级，不做过度设计）

### 7.1 推荐技术栈

- 语言：C++20
- 网络层：异步 I/O（可结合 `Coroutines`）
- HTTP：支持 `/chat` 与 `SSE`
- 服务间通信：HTTP + JSON（不使用 `gRPC`）
- LLM 接入：OpenAI-compatible HTTP API
- 配置：本地配置文件（YAML/JSON 二选一）

### 7.2 开发与运行环境约束（已确定）

- 开发方式：Cursor Remote SSH 连接 RHEL 虚拟机。
- 代码仓库：在虚拟机内使用 Git 提交并推送 GitHub。
- 运行与压测：统一在虚拟机内执行，保证环境一致性。
- 当前环境基线：
  - vCPU：4 核
  - RAM：15 GiB
  - Kernel：`5.14.0`
  - 根目录可用空间：18 GiB（需监控构建产物与日志）
  - 共享目录：`/mnt/hgfs`（仅用于文件互传，不作为主构建目录）

### 7.3 模块划分

- `C++ Gateway Service`
  - 路由分发（`/chat`）
  - 限流与调度
  - 请求日志
  - 转发到 Python Agent Service
- `Python Agent Service`
  - 任务编排
  - tool 决策与调用
  - memory 读写（可选；本版未落地）
  - LLM 调用封装与错误处理

---

## 8. 4 周里程碑

### Week 1：基础框架

- 搭建 C++20 项目骨架
- 完成 `/chat` 非流式闭环（C++ Gateway -> Python Agent -> LLM）
- 加入基础日志

### Week 2：流式能力

- 打通 `SSE` 返回
- 完成基础限流（`429` 生效）
- 补充异常处理路径

### Week 3：Agent 能力

- 接入 tool 调用（至少 1 个可演示）
- 完成 Agent 任务处理流程
- 可选：实现简单 memory（未纳入 `v1.0.0` 交付）

### Week 4：打磨与展示

- 压测与指标采集（吞吐、延迟、TTFT）
- 完成 `Coroutines + io_uring` vs `epoll + 线程池` 对比压测与结果整理
- 输出架构图、时序图、面试讲稿
- 修复稳定性问题，准备 demo 脚本

---

## 9. 验收标准（最终）

项目在单机可完成以下演示：

1. `/chat` 非流式调用成功。
2. `/chat` 流式 `SSE` 输出成功。
3. Agent 至少完成一次真实 tool 调用并返回结果。
4. 限流生效并返回 `429`。
5. 能展示并发压测结果与关键指标。
6. 能展示两种并发模型对比结果，并解释为何主线选择 `Coroutines + io_uring`。
7. 每个已完成模块都有对应的开发问题复盘，并写入 `TIL.md`。

---

## 10. 风险与取舍

- 若时间不足，优先保留：
  - `/chat`
  - `SSE`
  - 基础限流
  - Agent + 1 个 tool
- 可延后项：
  - 简单 memory
  - provider 扩展到多个
- 任何新增需求若触发“分布式/多租户/cost 计费/复杂策略”，一律延期到后续版本。

---

## 11. 面试讲述提纲（可直接复用）

1. 为什么做这个项目：AI 网关是高并发与 AI 集成的交叉点，能体现系统能力。
2. 核心设计：用户 -> 网关 -> Agent -> LLM 的最小闭环。
3. 技术亮点：`SSE` 流式、异步并发、基础限流、tool 调用。
4. 工程取舍：明确单机 MVP 边界，4 周内完成可运行与可压测版本。
5. 后续演进：在 MVP 稳定后再考虑更复杂能力，而非一开始过度设计。
